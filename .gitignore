# memfs / mvm — tool + skills repo. KB is regenerable (`mvm index`) and the
# resources slice is mirrored into alfred-home's ~/resources. State is local.
knowledge/
state/
.pytest_cache/
__pycache__/
*.pyc
*.pyo
.DS_Store
