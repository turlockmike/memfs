# memfs / mvm — engine + skills repo. The KB (knowledge/) and runtime state
# are the USER's data, never the repo's. test/ holds local corpus-specific
# eval fixtures, also untracked.
/knowledge/
/state/
/test/
# memfs auto-rendered directory indexes (regenerated by the watcher every pass;
# would perpetually dirty the repo). Hand-authored docs are README/ARCHITECTURE.
index.md
.pytest_cache/
__pycache__/
*.pyc
*.pyo
.DS_Store
